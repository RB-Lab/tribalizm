describe('Quest negotiation', () => {
    // it('notifies second party on quest proposal')
    // it('notifies first party on proposed changes')
    // it('notifes other party on acceptance')
    // it('notifes other party on decline')
    // it('doesn\'t allow to decline "initiation" quest')
})
